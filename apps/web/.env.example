# Postgres connection string used by Prisma (runtime + migrations).
DATABASE_URL=postgres://root:password@localhost:5432/aurora

# Secret used to sign the session cookie. Generate with: openssl rand -base64 32
SESSION_SECRET=change-me
